# 仓库智能管理系统 · 产品研究文档（research.md）

> **Day 3 产出**。本文档记录"我们要做什么、研究了多少同类产品、以及本期（28 天）只做哪一块"。
> 本项目**不是从零开始**，而是基于已完成的雏形「福州校区仓库管理系统」继续开发优化（现有代码库：`fuzhou-warehouse-system`）。

---

## 0. 一句话定位

一个**个人发起的开源项目**：用「前端网页 + 后端 + 3D 可视化」三件套，解决**小房间式仓库（类似杂物间，靠几个架子摆放物资）**"东西找不到、借还靠手工、只有收拾的人知道放哪"的问题。任何人（哪怕第一次来）都能在 3D 里自己找到物资在第几区、第几排、第几层、第几格。

> 注意：我们的"仓库"**不是快递大仓 / 物流中心**，而是一个**小房间 + 几组架子**的尺度。所以 3D 可视化的重点是"小空间精确定位"，不是物流级大仓调度。

---

## 1. 我的产品要解决什么问题（痛点）

| 痛点 | 具体表现 | 我们要怎么解决 |
| --- | --- | --- |
| **知识孤岛** | 只有收拾仓库的那个人知道东西放哪 | 3D 可视化 + 库位坐标，任何人可自助定位 |
| **流程不规范** | 借还不完善，东西乱放、不归位，一直在"熵增" | 前端借还流程 + 黑名单 + 拍照归还 |
| **手工记账累** | 每次有人借，管理员都要手工记录并更新 | 后端自动落库，借还在前端操作即增减 |

**核心卖点（差异化）**：别人做仓库系统是"表格 + 单据"，我们多了一层 **3D 空间定位**——把"翻表格找货"变成"看地图找货"。

---

## 2. 现有雏形已经做到哪（现状摸底，避免重复造轮子）

| 模块 | 文件位置 | 状态 | 说明 |
| --- | --- | --- | --- |
| 前端移动端原型 | `frontend/mobile-prototype` | ✅ 已端到端验证 | 浏览/搜索/借用/我的借还/拍照归还；连本地后端 `localhost:8005`，点"借"会真写测试副本 |
| 后端桥接服务 | `backend/prototype_server.py` + `tdoc_bridge/` | ✅ 已去硬编码 | 把腾讯文档 smartsheet 的 MCP 能力封装成 HTTP API；路径/file_id 全走环境变量 |
| 3D 可视化 | `3d/warehouse-v2`（主推） | ⚠️ 演示数据 | 26 条演示物资；5 类搜索、选中高亮+镜头聚焦、俯视小地图、面包屑路径、手机触摸适配 |
| 数据底座 | 腾讯文档 smartsheet（6 张表） | ✅ 临时顶替数据库 | 物资档案 / 出入库 / 借还登记 / 领用出库 / 黑名单 / 外来人员出入登记 |
| 库位坐标模型 | 演示数据字段 | ✅ 已有 | `校区 > 区域(A/B/C) > 排(1-3) > 层(1-3) > 箱位(A-01-02)` + `x/y/z` 三维坐标 |

**关键缺口（本期要补的）：**
1. 3D 还是**演示数据**——`adapter.js` 的 `loadFromTencentDocs()` 是占位，没接真实库存。
2. **单仓库**，没有多校区/多仓库。
3. **无硬件**（扫码枪 / RFID），纯手动操作。

---

## 3. 类似产品比较（2–3 个）

我挑了三类，正好对应我们系统的三个板块，也方便后面"取长补短"：

| 维度 | A. 传统进销存 SaaS（百草 / 秦丝 / 管家婆） | B. InvenTree（开源库存系统） | C. 3D 仓库可视化（astopaal/3d-warehouse、Simple-WMS-SSVM） |
| --- | --- | --- | --- |
| **目标用户** | 不懂技术的商贸/批发/零售小老板 | 工程师、制造商、技术团队 | 做可视化仓库的开发者、学习者 |
| **核心页面** | 商品列表、库存查询、出入库单据、报表 | 零件分类树、库存位置、BOM、采购/销售单 | 可旋转/缩放的 3D 仓库场景、货架点击、仪表盘 |
| **主要数据** | SKU、库存数量、批次、客户/供应商 | Parts（零件）、Stock Locations（位置树）、变动历史、批次/序列号 | 库位坐标（货架/层/格）、物料类型、库位状态 |
| **明显优点** | 上手极快、移动端扫码开单、**UI 好看**（百草）、行业适配深 | **数据模型极扎实**（层级分类+位置树+变动历史）、MIT 开源、可自托管、REST API | **直观看到货在哪**、交互炫、React+R3F / Vue+Three.js 现代栈 |
| **明显不足** | **完全没有空间/库位概念**（不知道货在第几层第几格）、无 3D、免费版阉割 | **无 3D 可视化**、偏制造业、部署重（Django+PostgreSQL+Redis） | 多为 demo、**业务深度浅**（没有单据/采购/报表）、数据常写死在前端 |

**我们的位置**：我们恰好在 A 和 C 的交叉点——既有 A 的"借还/黑名单/拍照归还"业务流程，又有 C 的"3D 定位"，还用 B 的思路做了底层"库位坐标 + 变动"数据模型。商业进销存（A）**都没做空间定位**，这正好证明我们的 3D 差异化是对的。

---

## 4. 哪个产品改变了我的想法？—— 诚实版

**结论：研究没有让我"推翻重来"，而是两件事：**

1. **确认方向没错**：看了 A 类（百草/秦丝）发现，商业进销存**根本没有"货在空间里哪"的概念**，只有表格和数量。这反过来证明我们"小房间货架 3D 定位"这个差异点是真实需求、不是自嗨——别人没做，我们做了就是壁垒。

2. **补齐了"参考系"（这才是研究的真正收获）**：原本我只有一团"想做 3D 找货"的模糊想法，研究后我知道了：
   - 数据模型该向谁学（InvenTree 的位置树、Simple-WMS 的库位表）；
   - 3D 交互该向谁抄（astopaal 的点击高亮+状态着色）；
   - UI 颜值该去哪找灵感（百草、Element Plus、Dribbble）；
   - 哪些坑别踩（InvenTree 部署重、3D demo 项目业务浅、Smart-WMS 半成品）。

> 一句话：我没被某个产品"点醒"，而是被**一群产品共同校准**了——知道该抄谁、该避开谁、我们的护城河在哪。

---

## 5. 各板块"取其精华、去其糟粕"（参考清单）

| 我们的板块 | 学谁 | 抄什么 | 不抄什么（糟粕） |
| --- | --- | --- | --- |
| **后端数据模型** | InvenTree + Simple-WMS | 位置树 + 变动历史的思路；四张表（仓库/库位/物资/变动） | InvenTree 的重部署（Django+PG+Redis） |
| **3D 可视化前端** | astopaal/3d-warehouse | 点击货架看详情 + 状态着色 + 镜头聚焦 | 它"数据写死在前端"的做法（我们要接真实数据） |
| **3D 库位精度** | Czehang/warehouse-design | 四级定位（区/架/层/格口）最贴合"找物资在哪" | —— |
| **万级库位性能** | hahzterry/warehouse-mapping-tool | CSV 导入 + InstancedMesh 批量渲染 | —— |
| **前端 UI 风格** | 百草（颜值）+ Element Plus / Ant Design（组件） | 干净后台布局、好看的表单与列表 | 不抄进销存的"满屏表格"压抑感 |
| **整体架构** | Simple-WMS-SSVM（最像我们） | React/Vue + 3D + 库位管理一体 | 它绑 Supabase 云后端——我们零基础先不绑云，用自托管轻后端 |
| **智能算法（远期）** | mohaneddz/Smart-WMS（ATLAS） | EMA 预测补货的"思路" | 它本身 "half baked"，本期不碰 |

---

## 6. 本期不做（MVP 边界 · 28 天内明确排除）

本期只聚焦把"**单仓库 + 3D 接真实数据 + 借还闭环**"跑顺，**以下明确不做**：

1. **多仓库 / 多校区管理**（目前只有 1 个仓库，坐标模型里 `campus` 先固定为福州校区）。
2. **扫码枪 / RFID 等硬件对接**（目前纯手动录入与借还，不接任何硬件）。
3. **采购单 / 销售单 / 财务对账**（当前只有借还登记 + 黑名单 + 拍照归还，不做进销存全链路）。
4. **高级报表批量导出**（Excel / PDF 大批量导出暂不做了）。
5. **AI 补货预测 / Blender 精致建模**（你以后想用 GPT 辅助做 Blender 建模，放远期；MVP 用 Three.js 基础几何体表达"货在哪"就够了）。
6. **接正式文档上线 / 写真实库存字段**（本期只在**测试副本**开发验证；触碰正式文档或写库存字段需单独评审授权，见红线）。
7. **迁移到 PostgreSQL / MySQL 正规数据库**（本期继续用腾讯文档 smartsheet 当底座，未来再平滑迁移）。

---

## 7. 公开参考资料（已联网核实真实存在）

**GitHub 开源项目：**
- InvenTree（开源库存，数据模型参考）：`https://github.com/inventree/InvenTree`
- astopaal/3d-warehouse（纯前端 3D 仓库，交互参考）：`https://github.com/astopaal/3d-warehouse`
- lordexilon/Simple-WMS-SSVM（React+库位管理+3D，最像我们）：`https://github.com/lordexilon/Simple-WMS-SSVM`
- Czehang/warehouse-design（Flask+Three.js，四级定位）：`https://github.com/Czehang/warehouse-design`
- hahzterry/warehouse-mapping-tool（CSV 导入 + InstancedMesh）：`https://github.com/hahzterry/warehouse-mapping-tool`
- mohaneddz/Smart-Warehouse-Management-System（ATLAS，AI 预测，仅学思路）：`https://github.com/mohaneddz/Smart-Warehouse-Management-System`

**在线 Demo / 文档：**
- InvenTree 在线 Demo（直接体验后台 UI）：`https://demo.inventree.org`（账号 `allaccess` / 密码 `nolimits`）
- TresJS 官方文档（Vue 3 声明式 Three.js，技术栈备选）：`https://docs.tresjs.org`
- 前端组件库（UI 参考）：Element Plus `https://element-plus.org`、Ant Design `https://ant.design`
- UI 灵感：Dribbble 搜 `warehouse dashboard` / `3D warehouse UI`

**本项目已有资料（研究基线）：**
- 发起人此前写的《福州校区仓库管理系统 · 项目研究说明》（`RESEARCH.md`，含完整架构、红线、踩坑手册）——本文档在其基础上精简并补入同类产品研究与本期边界。
- 现有代码库：`fuzhou-warehouse-system/`（`frontend` / `backend` / `3d` / `docs` / `data` 模块拆分，已 git 初始化）。

---

*研究结论：方向已验证（小房间货架 3D 定位是真实差异点），本期聚焦"3D 接真实数据 + 单仓库借还闭环"，把参考项目的精华按需取用、糟粕规避。*
