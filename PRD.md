# 仓库智能管理系统 · 产品需求文档（PRD.md）

> **Day 4 产出**。本文档是「施工图纸」——Day 7 写代码时照它来。
> 本项目**不是从零开始**，而是基于已完成的雏形「福州校区仓库管理系统」（`fuzhou-warehouse-system`）继续开发优化。
> **权威声明**：本文档是项目基线 `RESEARCH.md`（真实项目根目录）的**需求细化**；两者若有冲突（尤其涉及红线），**一律以 `RESEARCH.md` 为准**。
> 配套研究文档见 `research.md`（Day 3 产出），本文档的功能边界与砍功能说明以 research.md 第 3/6 节为准。

---

## 1. 背景与目标

### 1.1 我们做什么
一个**个人发起的开源项目**：用「前端网页 + 后端 + 3D 可视化」三件套，解决**小房间式仓库（类似杂物间，靠几个架子摆放物资）**"东西找不到、借还靠手工、只有收拾的人知道放哪"的问题。任何人（哪怕第一次来）都能在 3D 里自己找到物资在第几区、第几排、第几层、第几格。

### 1.2 明确不是什么
- **不是**快递大仓 / 物流中心——我们的"仓库"是小房间 + 几组架子的尺度，3D 重点是"小空间精确定位"，不是物流级大仓调度。
- **不是**从零重写——现有代码库已有前端借还闭环（已端到端验证）、后端腾讯文档桥接（已去硬编码）、3D 演示可视化。本期是"继续做 + 接真实数据"，不是重新发明。

### 1.3 本期（28 天）目标

**先看清起点**：借还闭环、拍照归还、3D 搜索定位（高亮 / 镜头聚焦 / 面包屑 / 小地图）在**演示数据 / 测试副本**上**已经跑通**（见第 3.1 状态列）——所以本期不是"从零做这些"，而是"**从演示走向真实**"。

**本期真正要推进的 4 件事：**
1. **3D 从演示数据 → 接入真实（副本）库存数据**：实现 `adapter.js` 的 `loadFromTencentDocs()`。
2. **把真实仓库建模进 3D**：将真实货架 / 箱位坐标映射到场景（坐标模型 `校区 > 区域 > 排 > 层 > 箱位` 已具备）。
3. **补上黑名单校验**：唯一尚未实现的功能。
4. **在真实数据上复验**已有功能（搜索定位、借还、拍照归还），确认不是"只在演示数据上好看"。

**不变的核心卖点**：**把"翻表格找货"变成"看 3D 地图找货"**。

---

## 2. 用户角色

| 角色 | 是谁 | 能干什么 | 不能干什么 |
| --- | --- | --- | --- |
| **管理员（你）** | 项目发起 / 维护者 | 管理物资档案、维护库位坐标、查看借还与黑名单、操作 3D 后台 | —— |
| **借物人** | 第一次来仓库的人 | 在 3D 里搜索物资、看到它在哪个箱位、发起借用、拍照归还 | 不能改库位/档案、不能移除黑名单 |
| **外来人员** | 非本组织访客 | 受限查看（或被黑名单禁止借用） | 不能借用、不能看到完整库存明细 |

---

## 3. 功能清单

### 3.1 本期做（MVP · 28 天内）

> 状态列：**已具备** = 现有雏形已实现；**本期做** = 本期需要完成。

| # | 功能 | 状态 |
| --- | --- | --- |
| 1 | **3D 搜物资位置**：在 3D 货架场景里输入 / 选择物资，镜头聚焦到对应箱位（校区 > 区域 > 排 > 层 > 箱位），高亮显示 | 已具备（演示数据） |
| 2 | **物资浏览与搜索**：列表 / 搜索查看物资名、类别、所在库位、是否有借出记录 | 已具备（演示数据） |
| 3 | **借用流程**：选物资 → 填借用人 → **生成一条借还登记记录**（不修改物资库存字段） | 已具备（测试副本已验证） |
| 4 | **拍照归还**：归还时拍照留证 → **置【实际归还日】** 并留痕（不改库存字段） | 已具备（测试副本已验证） |
| 5 | **黑名单**：对违规借用人标记黑名单，禁止其再借 | 本期做 |
| 6 | **3D 接真实（副本）库存数据**：把 `adapter.js` 的 `loadFromTencentDocs()` 从占位改成读取测试副本数据，替代写死的 Mock（不准的字段允许 Mock 兜底并标注） | 本期做 |

### 3.2 本期不做
见第 5 节「砍掉的功能与理由」。

---

## 4. 验收标准（可逐条打勾）

> 说明：✅ = 现状已验证（括号注明依据）；⬜ = 本期仍需达成。
> ⚠️ 凡依据标"演示数据"的项，需在本期接入真实（副本）数据后**复验**，才算最终通过。

- [✅] 打开 3D 页面，能看到仓库的货架 / 架子立体场景，可旋转、缩放、触摸操作。（现状）
- [✅] 输入物资名 / 编号，能在 3D 中搜到并**高亮**对应箱位。（演示数据已验证）
- [✅] 镜头自动聚焦到该箱位，并显示面包屑路径（例：福州校区 > A区 > 第1排 > 第2层 > 箱A-01-02）。（演示数据已验证）
- [✅] 借用流程走通：选物资 → 填借用人 → 提交 → **生成一条借还登记记录**（**不修改物资库存字段**）。（测试副本已验证）
- [✅] 拍照归还走通：上传照片 → **置【实际归还日】** 并留痕（**不改库存字段**）。（测试副本已验证）
- [⬜] 黑名单生效：标记某人后，其借用请求被拒绝。（本期做）
- [⬜] 3D 数据来自真实（副本）库存，而非写死的 Mock；**允许对不准的字段用 Mock 兜底，但须明确标注来源**。（本期做）
- [✅] 刷新页面后借还记录保留（借还登记已落腾讯文档）。（已验证）
- [✅] 单仓库坐标模型跑通：`校区 > 区域(A/B/C) > 排(1-3) > 层(1-3) > 箱位` 全链路可定位。（演示数据已验证）

---

## 5. 砍掉的功能与理由（今日要掌握：为什么砍）

本期只聚焦"单仓库 + 3D 接真实（副本）数据 + 补黑名单 + 已有功能在真实数据上复验"，以下**明确砍掉**，每条附理由：

1. **多仓库 / 多校区管理** — *理由：精力。* 目前只有 1 个仓库，坐标模型里 `campus` 先固定为福州校区；多仓库会带来组织/权限复杂度，非本期核心。
2. **扫码枪 / RFID 等硬件对接** — *理由：非核心 + 成本。* 当前纯手动录入与借还已够用；硬件涉及设备采购与驱动，零基础 28 天内不划算。
3. **采购单 / 销售单 / 财务对账** — *理由：非核心。* 当前只做"借还登记 + 黑名单 + 拍照归还"，进销存全链路是另一套业务，会严重稀释 3D 主线。
4. **高级报表批量导出（Excel / PDF）** — *理由：优先级低。* MVP 阶段人少货少，列表查看足够；批量导出是"锦上添花"，不是"雪中送炭"。
5. **AI 补货预测 / Blender 精致建模** — *理由：远期 + 技术前置。* 你以后想用 GPT 辅助做 Blender 建模，但 MVP 用 Three.js 基础几何体表达"货在哪"已足够；AI 预测需历史数据沉淀，现在没有。
6. **接入正式腾讯文档主文档 / 写真实库存字段** — *理由：数据安全红线 + 当前数据不准。* **本期只用"测试副本"开发验证**（副本是为防乱改导致正式数据丢失而建），且部分信息数据不准、仍允许用 Mock 假数据顶替；触碰正式文档或写真实库存字段需单独评审授权，放后续计划。
7. **迁移到 PostgreSQL / MySQL 正规数据库** — *理由：迁移成本 vs 收益。* 本期继续用腾讯文档 smartsheet 当底座，未来数据量上来再平滑迁移；提前迁移会打断主线。

---

## 6. 后续计划（MVP 之后再上）

- 接入**正式腾讯文档主文档** + 校准数据，消除 Mock 假数据。
- 多仓库 / 多校区管理（扩展 `campus` / `warehouse` 维度）。
- 平滑迁移到 PostgreSQL（用 Supabase 上云，接住"想上云"诉求）。
- AI 补货预测、GPT 辅助 Blender 精致 3D 建模。
- 报表批量导出、扫码枪 / RFID 硬件对接。

---

## 附录 A：用户故事示例（余力加练）

**功能：3D 搜物资位置**

> **作为** 第一次来仓库的借物人，
> **我想** 在网页 3D 场景里输入物资名字就能看到它摆在第几排第几层第几格，
> **以便** 不用问管理员、自己就能把东西找到并取走。

**验收场景：**
- 给定：仓库 3D 已加载，物资"电钻"存在于 A区-第1排-第2层-箱A-01-02。
- 当：借物人在搜索框输入"电钻"并回车。
- 那么：3D 场景镜头自动飞向该箱位并高亮，面包屑显示"福州校区 > A区 > 第1排 > 第2层 > 箱A-01-02"。

---

*PRD 结论：本期边界清晰——做"3D 定位 + 借还闭环 + 接副本数据"，砍掉 7 类非核心功能，把"接正式文档 / 上云 / AI 建模"留到后续。Day 7 照此施工。*
