# 运行说明（RUN.md）

> **Day 7 产出**。本文档记录「3D 仓库搜索定位」页面的启动方式、验证步骤与常见问题。
> **一句话**：本项目是一个**纯静态前端**页面，不需要安装任何依赖，用一个命令起个本地服务就能打开。

---

## 1. 这是什么

仓库智能管理系统的 **3D 可视化搜索页**（MVP 第一版）。

**它能做什么**：
- 看到仓库的 3D 货架场景（可旋转、缩放）
- 输入物资名 / 编号 → 对应的箱位**高亮**、镜头**自动聚焦**
- 显示面包屑路径，告诉你东西在「哪个区、第几排、第几层、哪个箱位」
- 支持手机浏览器访问（含触摸操作）

**界面只保留核心功能**（Day 7 精简）：
- ✅ 保留：**搜索** → **高亮** → **镜头聚焦** → **面包屑定位** → **全景按钮**
- ❌ 已移除：俯视小地图、2D/3D 切换、区域标签开关、手机触摸按钮（V2 的"花哨"功能）

**它现在用的是**：本地**演示数据**（26 条，界面右上角常驻「演示数据」徽标），不是真实库存。

**数据来源规划**：当前为演示数据；后续按 `TECH_DESIGN.md` 方案 X 接入后端 API（读腾讯文档测试副本）。

---

## 2. 启动方式

### 前提

只需要 **Python 3**（用来起一个静态文件服务）。本项目**不需要 `npm install`**，因为所有依赖（Three.js、Lucide）都已放在 `vendor/` 目录里。

> **为什么要起服务，不能直接双击 `index.html`？**
> 因为页面用了 ES Module（`type="module"`）和 `importmap`，浏览器的安全策略**禁止 `file://` 协议加载模块**。必须通过 `http://` 访问。这不是本项目的问题，是所有现代前端项目的通用限制。

### 启动命令

在**本目录**（`3d-warehouse/`）下执行：

```bash
python -m http.server 8010 --bind 127.0.0.1
```

如果你的 `python` 命令不可用，试试：

```bash
python3 -m http.server 8010 --bind 127.0.0.1
```

### 打开页面

启动后，浏览器访问：

```
http://127.0.0.1:8010
```

### 停止服务

在运行的终端里按 `Ctrl + C`。

---

## 3. 验证步骤（自己确认跑通了）

按顺序做，每步都应该看到对应结果：

| # | 操作 | 应该看到 |
| --- | --- | --- |
| 1 | 执行启动命令 | 终端出现 `Serving HTTP on 127.0.0.1 port 8010` |
| 2 | 浏览器打开 `http://127.0.0.1:8010` | 页面加载出 3D 货架场景（A/B/C 三个区的立方体箱子） |
| 3 | 看页面顶栏 | 标题「福州校区仓库 · 3D 定位工作台」+ 右上角「演示数据」徽标 |
| 4 | 搜索框输入 `不锈钢餐盘` 回车 | 3D 里某个箱子亮起（橙色描边），镜头飞过去，显示面包屑路径 |
| 5 | 看面包屑 | 形如「福州校区 > A区 > 第1排 > 第2层 > 箱A-01-02」 |
| 6 | 拖拽鼠标 / 滚轮 | 场景可旋转、可缩放 |

**第 4 步是核心**——它验证了「把"翻表格找货"变成"看 3D 地图找货"」这个卖点。

> **代码可维护性（Day 7 重构）**：页面逻辑已按「高内聚低耦合」拆成 13 个小模块
> （每个文件只管一件事，最长的 `scene.js` 也只有 237 行），`main.js` 从 625 行瘦身到 163 行，
> 只承担"接线与启动"。详见第 4 节的模块结构图。

### 手机 / 平板怎么操作

**方式一（推荐）：直接用在线链接**

已发布一份云端演示版，手机浏览器打开即可，无需在电脑上起服务：

```
https://e887bac984dd46d8a6df125eacdce7d8.sg.agentos-app.run
```

> 在线版是**纯静态 + 演示数据**（不含后端），用于快速体验 3D 找货效果。
> 后续如果改了代码想同步到线上，需要重新发布（见第 9 节）。

**方式二：局域网访问本机服务**

用手机浏览器打开 `http://<电脑局域网IP>:8010`（需在同一 WiFi 下），或直接把 8010 端口映射到手机可访问的地址。

触摸手势如下：

| 手势 | 效果 |
| --- | --- |
| **单指拖动** | 旋转场景 |
| **双指捏合 / 张开** | 缩放（拉近拉远） |
| **双指拖动** | 平移场景 |
| **单指长按后拖动** | 平移场景（手机没有"右键"，用长按代替） |

> **手机适配说明**：搜索框与按钮在手机上加大到 44px（手指点按的推荐最小尺寸）；搜索框字号设为 16px，避免 iOS Safari 在聚焦时自动放大页面；结果显示区在窄屏下变为**底部抽屉**，把更多空间留给 3D 场景。

### 演示数据里可搜的物资（部分）

| 物资名 | 箱位 |
| --- | --- |
| 不锈钢餐盘 | A-01-02 |
| 餐刀 | A-01-01 |
| 砧板 | A-01-03 |
| 筷子 | A-02-01 |
| 一次性吸管 | A-02-02 |

完整 26 条见 `data/demo-data.json`。

---

## 4. 目录结构

```
3d-warehouse/
├── index.html              # 页面入口（含 importmap 配置）
├── styles.css              # 样式（含手机竖屏适配）
├── package.json            # 只记录"怎么起服务"，无 npm 依赖
├── data/
│   ├── demo-data.json      # 演示数据（26 条，标 dataStatus: 演示数据）
│   └── seed.json           # 后端 seed 模式用的快照（由 demo-data.json 生成）
├── backend/                # 轻后端（Day 7 新增，方案乙）
│   ├── server.py           # HTTP 服务：两种模式（seed 快照 / 腾讯文档真实）
│   └── README.md           # 后端说明 + 已发现的数据缺口
├── src/
│   │  ── 核心 ──────────────────────────────────────────
│   ├── main.js             # 组装层：接线 + 绑定界面事件 + 启动
│   ├── config.js           # 全局配置常量（颜色 / 尺寸 / 视角 / 手感参数）
│   ├── state.js            # 共享运行时状态（会被改动的数据集中在此）
│   ├── layout.js           # 坐标唯一数据源（区域/排/层 → 世界坐标）
│   │  ── 渲染 ──────────────────────────────────────────
│   ├── scene.js            # 渲染基建 + 场景构建（渲染器/相机/灯光/箱位/标签）
│   ├── camera.js           # 相机运动（参数 → 位置；flyTo 平滑过渡）
│   ├── mapview.js          # 全屏俯视地图（进出、点选换算）★ Day 8 新增
│   ├── minimap.js          # 小地图（独立模块，当前界面已停用）
│   │  ── 业务 ──────────────────────────────────────────
│   ├── search.js           # 搜索匹配规则（输入 → 命中记录）
│   ├── emphasis.js         # 高亮与定位（强调哪个箱位 + 聚焦 + 复位）
│   │  ── 界面与接口 ────────────────────────────────────
│   ├── panel.js            # 界面 DOM 的唯一出入口（列表/面包屑/详情卡）
│   ├── controls.js         # 视角输入（鼠标拖拽 + 触摸手势）
│   ├── adapter.js          # 数据适配层（后端 API / 腾讯文档 → 标准结构）
│   └── api.js              # 对外测试接口（window.__diag / __api）
├── verify/                 # 自动化验收脚本 + 截图（非运行时依赖）
│   ├── verify-refactor.mjs  # 重构验收：22 项端到端断言
│   ├── verify-mobile-ux.mjs # 手机体验验收：110 项断言（手感/详情卡/地图/自适应/四态/标记环/抽屉/横屏三档）
│   ├── verify-api.mjs       # 方案乙验收：7 项断言（含降级路径）※需先切 api 模式
│   └── shots/               # 跑脚本自动生成的截图（已 gitignore，不进仓库）
│       └── shot-*.png       #   桌面 / 手机 / 地图 / 走后端
├── shot-mainview-with-url.png  # 打卡交付截图（带地址栏，故意留在仓库里给人看）
├── shot-marker-none.png        # 同上：标记环修复后的无搜索主视图
├── vendor/                 # 第三方库（本地内置，无需联网）
│   ├── three.module.js     # Three.js 引擎
│   ├── three.core.js
│   ├── lucide.js           # 图标库
│   └── addons/             # Three.js 官方后处理（高亮描边等）
├── REUSE.md                # 开源复用记录（许可证来源）
└── V2-REPORT.md            # 原 V2 版本的交付报告
```

### 模块之间怎么关系（依赖方向单向向下）

```
                    main.js（组装层）
                        │
      ┌─────────┬───────┼────────┬─────────┬────────┐
      ▼         ▼       ▼        ▼         ▼        ▼
  controls  search  emphasis  panel   mapview    api
      │         │       │        │        │
      ▼         ▼       ▼        │        │
   camera ←─────┴───────┴────────┼────────┘
      │                          │
      ▼                          │
   scene ────────────────────────┘
      │
      ▼
  config / state / layout（最底层，无内部依赖）
```

**规则**：箭头只允许向下，不允许反向或成环。想加功能时，先想清楚它属于哪一层，
再加到对应文件里 —— 这样删减或替换某一层不会牵动其他层。

举例：想换掉小地图，只动 `minimap.js`；想换界面样式，只动 `panel.js`；
想改搜索规则（比如支持拼音首字母），只动 `search.js`；
不想要全屏地图，删掉 `mapview.js` + `main.js` 里几行调用即可，其他模块不受影响。

### 数据从哪里来（适配层的位置）

```
   腾讯文档智能表  ──┐
                    ├──►  backend/server.py  ──►  adapter.js  ──►  3D 页面
   本地 seed 快照  ──┘        （持凭据 / 翻译字段）   （翻译成标准结构）
```

前端**永远不直接接触数据源**。换数据源只改后端，前端一行不动。

---

## 4.5 为什么要有一个后端（可选读）

对齐 `TECH_DESIGN.md` 选定的**方案 X**。你可能觉得"一个 3D 页面为什么要后端"，三条理由：

| 理由 | 具体是什么 |
| --- | --- |
| **凭据不进浏览器** | 访问腾讯文档需要密钥。本仓库是公开仓库，密钥写进前端 = 全网可见。放服务端才安全。 |
| **业务规则前移** | 字段翻译、脏数据过滤、编号反推都在后端做完，前端只管显示，代码更薄。 |
| **前端可替换** | 从"本地演示数据"换到"真实数据"，前端只改 `config.js` 里一个常量。 |

**怎么切换**（改 `src/config.js` 一行）：

```js
export const DATA_SOURCE = 'demo';   // 离线读本地 JSON（默认）
export const DATA_SOURCE = 'api';    // 走后端
```

**怎么启动后端**：

```bash
python backend/server.py             # seed 模式，端口 8011，无需任何凭据
```

> **降级保护**：`api` 模式下如果后端没启动，前端会自动退回本地演示数据并在控制台告警，
> **页面不会白屏**。这条降级路径已写进自动化测试。

后端细节（接口、凭据配置、**已发现的数据缺口**）见 `3d-warehouse/backend/README.md`。

## 5. 常见问题

### Q1：直接双击 `index.html` 打不开 / 3D 不显示

**原因**：浏览器禁止 `file://` 协议加载 ES Module。
**解决**：必须用 `python -m http.server` 起服务，通过 `http://127.0.0.1:8010` 访问。

### Q2：终端提示 `Address already in use`（端口被占）

**原因**：8010 端口已被别的程序占用。
**解决**：换个端口号，例如 `python -m http.server 8020 --bind 127.0.0.1`，然后访问 `http://127.0.0.1:8020`。

### Q3：页面打开是空白 / 3D 区域黑的

**排查顺序**：
1. 按 `F12` 打开控制台，看有没有红色报错
2. 确认 `vendor/` 目录完整（应含 `three.module.js`、`three.core.js`、`lucide.js`、`addons/`）
3. 确认 `data/demo-data.json` 存在
4. 硬刷新页面（`Ctrl + F5`）

### Q4：搜索没反应 / 搜不到

**排查**：
1. 确认搜的是演示数据里的物资名（见第 3 节表格）
2. 看控制台是否有 `fetch` 报错（说明 `data/demo-data.json` 没加载成功）

### Q5：为什么界面显示"演示数据"？

**这是设计如此**，不是 bug。当前接入的是本地 `demo-data.json`（26 条假数据），界面用徽标明示**数据来源**，避免误以为是真实库存。接入真实数据后该标记会改为"真实"。

---

## 6. 与其他文档的关系

| 文档 | 关系 |
| --- | --- |
| `TECH_DESIGN.md` | 技术路线来源：本页面对应其中「方案甲 · 纯本地」模式；后续按「方案乙」接后端 |
| `PRD.md` | 功能边界：本页面实现 PRD 3.1 节第 1、2 条（3D 搜物资位置 + 物资浏览搜索） |
| `AGENTS.md` | 协作规则：本文档遵循其中第十三节「改文档后必查红线」（已确认无敏感信息） |
| `REUSE.md` | 开源合规：本目录第三方库的许可证与来源记录 |

---

## 7. 已知限制（诚实记录）

| 限制 | 说明 | 计划 |
| --- | --- | --- |
| 数据是演示数据 | 26 条假数据，非真实库存 | 后端已就绪（方案乙），配好凭据即可切真实数据 |
| **档案表缺「层」字段** | 物资档案有区域/箱位/排，但**没有层**；当前从箱子编号反推，规则待现场核对 | 见 `backend/README.md` 第 5 节，需业务确认 |
| 无借还功能 | 本页面只做"找东西"，不含借还流程 | 借还在 `frontend/mobile-prototype`（另一模块） |
| 后端无鉴权 | 仅监听 `127.0.0.1`，本机使用 | 公网部署前必须加 |
| 在线版无后端 | 云端演示只发布纯静态前端（单端口限制），不含 API | 需带后端的版本请本机运行，见第 4.5 节 |
| 在线版是公开链接 | 任何人拿到链接都能访问 | 演示数据无隐私风险；接入真实数据后须改鉴权或下线 |
| 地图上定不到层 | 俯视时同一排的三层箱子垂直重叠，射线只能打到最上层；点货架只能定位到**排**，层由 3D 视图呈现 | 是俯视投影的固有特性，非缺陷；如需精确到层可在点上后弹出层选择 |

---

## 7.5 手机端手感调优记录（Day 8）

拿到真机反馈后修的六个问题。都改在已有模块里，没新增数据字段、没碰后端。

| 问题（真机反馈） | 原因 | 改法 | 改哪个文件 |
| --- | --- | --- | --- |
| 一次滑动转不了多少，要划好多下 | 旋转系数写死 `0.005`，手机屏幕窄、滑不了那么多像素 | 系数分设备：鼠标 `0.005` / 触摸 `0.011`（按 `pointerType` 自动选） | `config.js` + `controls.js` |
| 详情卡挡住大半页，只剩小半屏能滑动 | 详情卡一显示就撑满，且面板 `max-height` 是"只高不缩" | 详情卡改成**可收起且默认收起**（只占一行），点标题行展开；手机端面板上限 48vh → 40vh | `panel.js` + `styles.css` + `index.html` |
| 路径条不能点，没法看全屏地图 | 面包屑是纯 `div` 没有监听；俯视入口在砍花哨功能时删掉了 | 面包屑可点 + 左下补回「地图」按钮 + 按 `M`；全屏俯视下**点货架定位到那一排、点空地跳过去**；左上按钮变「返回 3D 视角」 | `mapview.js`（新建）+ `main.js` |
| 手机上 C 区标签被右边缘切掉 | 全景距离写死 52，竖屏水平视野只有约 23°，装不下 A~C | 全景距离**按画布宽高比反算**（见下面 7.5.2），俯视相机视野也按比例算 | `config.js` + `camera.js` + `scene.js` |
| **转到边上就卡住，不能一直转** | 纵向 `phi` 用 `clamp` 硬夹，到边界那一帧直接停死；且没有惯性，大角度得倒腾好几回手指 | 纵向边界处改**平滑阻尼**（越转越沉、自然到头）+ 新增**轻扫惯性**；长按平移改为**双指拖拽**（见 7.5.1） | `config.js` + `camera.js` + `controls.js` |
| **手指在画布上滑动，整个页面跟着滚** | `touch-action: auto` 没接管触摸 + 只有 `body` 设了 `overflow:hidden`（移动浏览器会把滚动容器提升到 `html`）+ `100vh` 取"地址栏收起时"的高度 | `html,body` 双 hidden + `overscroll-behavior:none` + `body{position:fixed;inset:0}` + 画布 `touch-action:none` + `100vh→100%`（见 7.5.3） | `styles.css` |

### 7.5.1 「无限拖拽」怎么做的

先分清一件事：**横向和纵向的"无限"不是一回事。**

- **横向（偏航 `theta`）本来就是无限的** —— 球坐标里偏航没有物理含义的上下限，可以一直转下去。之前唯一的隐患是数值一路增大（同一朝向有无数个等价值），现在归一化到 `(-π, π]`，朝向与数值一一对应。
- **纵向（俯仰 `phi`）不可能真正无限** —— `phi` 是相机与"正上方"的夹角，`phi=0` 时相机在头顶垂直俯视，此时 lookAt 的"上方向"退化成奇点，画面会翻转抖动；`phi` 到 90° 时相机落到地平线，会钻进地面看到货架背面。**任何 3D 软件都是到两端停住**，这不是本项目没做好。能做的是把范围开到接近垂直，并且**在两端平滑减速**。

所以纵向改成 `dampToBounds()`：越靠近边界，每一步的位移按"离边界还剩多少"同比削减。手感是"越转越沉、自然到头"，而不是"啪一下卡死"。实测贴边时一步位移被削到中段的 **0.03 倍**。

边界值写在 `config.js`：`PHI_MIN = 0.05`、`PHI_MAX = π/2 − 0.02`（两端各留约 3° 余量）。

**另外补了轻扫惯性**：快速一拨抬手后，镜头按最后的速度继续转一会儿，`friction = 0.94` 逐帧衰减。用 `rAF + 时间差` 推进并把 `dt` 折算成帧数，保证 60 / 90 / 120Hz 屏幕衰减速度一致。

**长按平移删掉了。** 原来"长按 350ms 再拖"才能平移，这个门槛在真机上很难用 —— 手指微抖就超过滑动阈值、系统手势也容易打断。手机上平移改为**双指拖拽**（和地图类应用一致），比长按可靠得多。桌面端保留右键平移。

### 7.5.2 全景距离改成"反算"而不是分档

竖向手机屏的可视**水平**半宽 = 距离 × `tan(25°)` × `aspect`。手机竖屏 `aspect ≈ 0.6` 时，水平半宽只有距离的 **0.28 倍** —— 这就是写死 52 装不下 A~C 的数学原因（A 区 x=−22、C 区 x=+22，还各带货架宽度）。

原来的"分档表"（`< 0.75 → 66`）实测只到 24.8，差一点。现在直接反算：

```
距离 = OVERVIEW_HALF_W / (tan(FOV/2) × aspect)
```

`OVERVIEW_HALF_W = 27`（含余量）、`CAMERA_FOV_DEG = 50`（与 `scene.js` 里的相机一致），再 `Math.ceil` 向上取整、并对宽屏取 `max(52, ...)` 兜底。好处是**任何屏幕比例都自动正确**，不用维护一张分档表。

实测：竖屏 `aspect=0.6` → 距离 **97**，货架四角投影到 NDC 的 x 落在 `[-0.604, 0.752]`（都在 ±1 内）；宽屏仍是 **52**（基准没被推远）。

### 7.5.3 页面为什么会被"滚走"

真机现象：手指在 3D 画布上向上滑，除了视角在转，整个页面也往上位移，顶部地址栏收起、3D 区被压缩；松手又弹回来。原因是四层叠加，**必须同时堵**：

| 层 | 原来 | 现在 | 为什么 |
| --- | --- | --- | --- |
| 触摸归属 | 画布 `touch-action: auto` | `#canvas-host, #canvas-host canvas { touch-action: none }` | 不接管的话，浏览器有权把纵向滑动解读成"滚动页面 / 下拉刷新"，和画布手势抢事件 |
| 滚动容器 | 只有 `body { overflow: hidden }` | `html, body { overflow: hidden }` | **只设 body 无效** —— 部分移动浏览器会把滚动容器提升到 `html` 上，body 那条形同虚设 |
| 回弹 / 下拉刷新 | `overscroll-behavior: auto` | `overscroll-behavior: none` | 关掉横向回退（前进后退手势）与下拉刷新 |
| 视口高度 | `#app { height: 100vh }` | `height: 100%` + `body { position: fixed; inset: 0 }` | 移动端 `100vh` 取的是"**地址栏收起时**"的高度，地址栏一出现内容就被撑出屏幕 —— 正是"页面被顶下去"的那个截图。`position: fixed` 则让页面在布局层面就不可能被滚动 |

> 只改前两层、或用 `100vh`，都还会漏。四条要一起上。

### 7.5.4 顺手修掉的两个隐性缺陷

**① 地图模式下结果项点不到（真实可用性缺陷）。** `body.map-mode .panel` 的 `max-height` 压到 `92px`、`.results` 压到 `48px`，而结果项本身高约 **101px** —— 整项被裁在面板外。元素还在、"看起来"也在，但 `elementFromPoint` 命中不到它，**手指点上去毫无反应**。用户可见后果是：搜「杯子」命中两条自动进地图后，**根本点不到结果项跳转到具体某一条**。

改成 `panel: 168px`（够容纳一项 + 面板头）、`.results: 104px + overflow-y: auto`（多条时能滑），地图仍拿到大部分画面。测试里加了一条断言专门守这个：检查结果项中心点 `elementFromPoint` 拿到的就是它自己。

**② 用户拖动会中断飞行动画。** 见下面"三个隐性 bug"第 3 条。

**顺手修掉的三个隐性 bug**（都是"平时不发作、一旦发作就整个页面转不动"）：

1. `dom.setPointerCapture()` 没包 `try/catch` —— 某些环境会抛 `NotFoundError`，一旦抛出就中断 `onDown`，导致 `mode` 没设上，**之后完全无法旋转**。
2. 指针残留自愈 —— 如果某次 `pointerup` 丢失（手指滑出屏幕、被系统手势打断、切后台），`pointers` 里会留下"幽灵触点"使 `pointers.size` 一直是 2，**后续所有单指操作都被当成双指**。现在在 `isPrimary` 触点按下时清理上一轮的残留。
3. **飞行动画会吞掉拖动**（这条最隐蔽，也最影响手感）—— `stepAnimation()` 每帧都会把 `view.theta` / `view.target` 重写成"起点→终点"的插值结果。只要动画还没播完，用户这时拖动手算出的角度**下一帧就被动画覆盖**，表现就是"手指明明在动、画面却几乎不转，要划好几下才动一下"。这正是"要划好多下"的第二个原因（和灵敏度是两回事）。修法：`rotateBy()` / `panBy()` 开头调用 `stopFly()`——用户主动拖动即视为接管镜头，立刻中断动画。

> 补记：`theta` 现在会归一化到 `(-π, π]`。此前一路转下去数值会无限增大（同一个朝向有无数个等价值），虽然单纯渲染看不出问题，但会让"比较两次朝向"变得不可靠。归一化后朝向与数值一一对应。

### 7.5.5 结果面板的四种状态（Day 8 补）

右侧「定位结果」面板现在有四种状态。**判定顺序不能调换**：加载中 → 出错 → 空 → 有结果。

| 状态 | 长什么样 | 什么时候 |
| --- | --- | --- |
| **加载中** | 转圈 + 「正在读取物资数据…」+ 两块骨架占位 | 数据还在路上 |
| **出错** | 暖红底 + 「物资数据没能读出来」+ 具体原因 | 数据读取失败 |
| **空** | 中性灰提示语（搜过没命中 / 还没搜，两种文案） | 没有结果 |
| **有结果** | 物资卡片列表（编号 / 名称 / 位置 / 区域徽标） | 命中 ≥1 条 |

**为什么加载态必须做** —— 这是四种里最容易漏的一个，因为 demo 模式读本地 JSON 快得看不见它。但两种情况下它会真的露出来：

1. 换成后端（`DATA_SOURCE = 'api'`）后，网络慢时会有肉眼可见的等待；
2. 手机上首次打开，光解析 three.js（约 1.2MB）就要一会儿 —— 这时面板如果显示的是**空态**文案（"输入…开始定位"），会给用户"页面已经就绪、数据已经在了"的错觉，然后 3D 场景迟迟不出来。

**为什么出错态要和空态拉开配色** —— 空态是"一切正常，只是没输入"，出错是"需要你注意"。两者都用中性灰的话，用户会把错误当成普通提示一扫而过。出错态用暖红（`--highlight-soft`）+ `role="alert"`（读屏会打断播报），空态用中性灰 + `role="status"`（读屏平静播报）。

**一个真实缺陷回归锁**：数据出错时 `state.items` 是空的，搜索必然返回 0 条。如果不拦一下，用户搜「杯子」会看到「未找到"杯子"对应的物资」—— 于是去改关键词反复重搜，而真正的问题是数据根本没读出来。现在这种情况会明确提示「暂时搜不了：物资数据没能加载成功」。

> **清错误不能用 `setLoadError(null)`**：它内部 `msg || '数据读取失败。'` 会把 `null` 填成默认文案，
> 于是"清错误"变成了"换成另一条错误"，页面卡在错误态出不来。必须走 `clearLoadError()`。

### 7.5.6 可复用的卡片组件（Day 8 余力加练）

把「物资卡片」从 `panel.js` 里抽出来，独立成 `result-card.js`。抽出来的直接好处是**同一张卡片能在两个地方复用**：

| 形态 | 文件 | 用在哪 | 为什么是这个形态 |
| --- | --- | --- | --- |
| **DOM 版** | `result-card.js` → `createItemCard()` | 右侧面板 | 需要可点、可键盘聚焦 |
| **Canvas 版** | `card-canvas.js` → `drawCardToCanvas()` | 3D 场景（悬浮卡片贴图） | three.js 的 Sprite 只认 canvas/图片纹理，**不认 DOM 节点** |

两者共享同一份字段顺序与配色规则（`CARD_COLORS`），改样式只改一处，不会出现"面板改了、3D 那版忘了改"。

抽出来的三个产物：

- `createItemCard(item, {onPick, compact})` —— 一条记录 → 一个可点卡片。带 `role="button"` + `tabIndex=0`，**键盘 Tab 到、回车能触发**（不只是鼠标能点）。
- `createHintCard(text, {kind, title})` —— 空/加载/错误三态共用一个结构，靠 `kind` 决定配色和读屏角色。
- `createSkeletonCard()` —— 加载中的骨架占位，形状接近真实卡片，让"即将出现什么"有预期。

**顺带修掉一个注入隐患**：原来渲染卡片是拼 `innerHTML` 字符串插入物资名称的 —— 名称来自数据源（将来可能是后端），里面带 `<` 就会破版。改成 `createElement` + `textContent` 后没有这个问题。详情卡的键值行也一并换掉了。

**无障碍细节**：转圈和骨架都标了 `aria-hidden="true"`（纯装饰，读屏跳过），真正的语义由外层 `role="status"` / `role="alert"` 承担；并且在 `prefers-reduced-motion: reduce` 下关掉转圈和呼吸动画（前庭功能敏感的用户会因循环动画不适，这是系统级偏好，值得遵守）。

**新增交互一览**：

| 操作 | 效果 |
| --- | --- |
| 单指拖拽 | 旋转视角（横向可**无限**转；纵向转到底会自然减速） |
| **快速轻扫后抬手** | 带惯性继续转一会儿，大角度不用反复划 |
| **双指拖拽** | 平移画面（取代原来的"长按 350ms 再拖"） |
| 双指捏合 | 缩放 |
| 桌面端右键拖拽 | 平移（鼠标保留，手机上无对应操作） |
| 点顶部路径条 / 点左下「地图」/ 按 `M` | 进入全屏俯视地图 |
| 在地图上点货架 | 定位到那一排并回到 3D |
| 在地图上点空地 | 镜头平移过去看 |
| 再点路径条 / 再点「地图」/ 按 `M` / 按 `Esc` | 退出地图，还原进地图前的视角 |
| 搜索命中**多条** | 自动切全屏地图，所有命中箱位一起高亮，一眼看清分布 |
| 点详情卡标题行「当前箱位」 | 展开 / 收起完整信息 |
| **飞行途中拖动** | 立刻中断动画，由手指接管（见上面第 3 条） |

### 7.5.7 地面标记环只在有目标时出现（真机反馈修复）

**现象**：没搜索任何东西时，空地正中央也杵着一个红圈，看着像"哪里出错了"。

**根因**：地面标记环（`state.marker`，那个红色圆圈）的语义是**"目标箱位在这里"**。
但它是建好就可见的，复位时也只把它挪回场景中央、**没有隐藏** —— 于是"没有目标"这个状态
反而留下了一个最像"有目标"的视觉符号。

**修法**（三处，缺一不可，任一漏掉都会让红圈在某个状态下冒出来）：

| 位置 | 改动 | 时机 |
| --- | --- | --- |
| `scene.js` → `buildMarker()` | `marker.visible = false` | 页面刚建好时**藏** |
| `emphasis.js` → `locate(item)` | `marker.visible = true` | 定位到某条物资时**显** |
| `emphasis.js` → `resetView()` | `marker.visible = false` | 复位时**藏** |

**验证方式**：读 `state.marker.visible` 三个时机各一次，断言分别是 `false` / `true` / `false`。
（`verify-mobile-ux.mjs` 已固化为三条断言，任何一处改错都会被拦下来。）

### 7.5.8 每次进地图都从"看全貌"开始（顺带挖出的真 bug）

**现象**：在地图里拖动过之后，退出再进地图，画面停在**上次拖走的位置**，不是全貌。

**根因**：`enterMap()` 只在第一次进来时把俯视相机归位到场景中心。
而俯视相机的目标点 `view2d.target` 会被两件事改掉：

1. 在地图里拖动平移（`panBy()` 在俯视模式下改的就是它）
2. **任何一次 `flyTo` 动画** —— `camera.js` 里每帧都会把 `view2d.target` 一起插值走，
   哪怕当时根本不在俯视模式（这是最隐蔽的一条）

于是带着残留值进地图 → 画面偏在一边，用户得自己拖回来，与"进来先看全貌"的预期不符。

**修法**：`enterMap()` 里先 `stopFly()` 掐掉还在写值的动画，再**无条件**重置 `view2d.target`。

**为什么值得记一笔**：这个 bug 是被一条**偶发失败的断言**带出来的
（"点选右侧位置飞到 C 区"时报 `target=[4.44,…]`，阈值是 `>5`）。
如果当时顺手把阈值从 `>5` 改成 `>3`，就会把一个真实的相机缺陷掩盖掉。
**判断方法**：单独跑这条断言 → 通过；跑完整流程 → 失败 ⇒ 一定是前面某步留下的状态，
要去打印"现场"（当时的相机/画布/全局状态），而不是改断言。


### 7.5.9 手机横屏：换一种排法，而不是把竖屏压扁

**问题**：竖屏下"顶栏 + 3D 舞台 + 底部面板"三段竖着排，3D 的净高本来就只有 300px 上下 ——
一边是搜索栏、一边是结果卡，货架被夹在中间一条窄缝里。

**思路**：横屏恰好相反，**纵向稀缺、横向富余**。所以不是"把竖屏布局压扁"，
而是把底部面板挪到**侧边**变成一列，3D 舞台独占剩下的整块区域：

| | 竖屏 | 横屏 |
| --- | --- | --- |
| 3D 舞台占屏高 | ~40% | **~87%** |
| 面板位置 | 底部（占高度） | **左侧栏**（占宽度） |

**判定条件用 `height` 而不是 `orientation`**：真正稀缺的是**高度**。
一个 900×420 的窗口和 800×500 的横屏手机，需要的是同一种布局；
用 `max-height: 560px` 判据能同时覆盖两者，也不会因为软键盘弹出（视口变矮）而误判成"横屏"。
再叠一个 `orientation: landscape` 兜住"高度不算矮但确实是横屏"的情况。

**横屏下把手必须收起来**：横屏是"侧栏"形态，不存在底部抽屉。
这条写在 CSS 的横屏块里（`display: none !important`）而不是只靠 JS 判断 ——
即使脚本没跑到，横屏也不该露出一根拖不动的把手。

#### 真机反馈后补的三处修复（Day 8 第二轮）

用户拿着横屏截图反馈了三点，其中**两点是真 bug**，记在这里：

**① 顶栏只占左半边 → 画面像被"劈成两块"**

原写 `.topbar { grid-column: 1 }`，顶栏只占第一列，于是**右列顶部（第一行第二列）
空出一块背景色**。视觉上就是"左边一块 3D、右边一块列表、上面还缺一角"，
用户原话是"感觉像是分割在两侧"。
→ 改成 `grid-column: 1 / -1` 横跨整行，顶部变成一条完整横栏，
下面才是 3D + 侧栏两列，整体成为一个"L 形"框架。

**② 横屏下面板只占 144px，下面一片空白（最隐蔽的一个）**

现象：侧栏只占上半截，下面 167px 全是空白；点结果回 3D 后更糟 ——
`.results` 被压到 **48px**，一条 119px 的结果卡**完全显示不出来**，
用户原话是"旁边的列表收缩了，只有很小一块地方"。

根因是**两个媒体查询的重叠区间**：

| 规则 | 位置 | 条件 |
| --- | --- | --- |
| 抽屉 `.panel[data-drawer] { height: var(--drawer-h, 40vh) }` | `@media (max-width: 820px)` | 窄屏 |
| 横屏 `.panel { height: 100% }` | `@media (orientation: landscape) and (max-height: 560px)` | 横屏 |

`740×360` 的横屏手机**两个条件都命中**。而抽屉那条优先级 (0,2,0) **高于**
横屏那条 (0,1,0) —— 优先级赢过书写顺序，于是 `height: 100%` 被无声推翻，
高度回落到 `40vh` = 144px。

→ **修法：抽屉规则一律加 `orientation: portrait`**，与 `panel.js` 的
`drawerApplies()` 条件保持**完全一致**（那边是
`max-width: 820px && !(landscape && max-height: 560px)`）。
两边条件一旦不一致，就会出现"JS 以为该有抽屉、CSS 以为不该有"的错位。
同理，`body.map-mode .panel[data-drawer] { height: 168px }` 那条也要加 portrait，
否则 600×360 这类极窄横屏的地图模式会把侧栏也压矮。

**为什么 844×390 测不出来**：844 > 820，不匹配抽屉那条，所以**完全正常**。
只测一个宽度就永远发现不了 —— 这也是下面 8.1 第 7 条的由来。

**③ 侧栏太窄（200px），卡片被挤到频繁换行**

原写 `grid-template-columns: 1fr minmax(200px, 34%)`。
`1fr` 会贪婪吃掉所有富余空间，于是 `minmax` 那一列**总被压到最小值** ——
740 宽的横屏上侧栏只有 200px（而不是 34% 的 251px），
卡片高度从 101px 涨到 119px，列表更放不下。
→ 改用 `1fr clamp(240px, 38%, 420px)`，宽度确定：
740 → 281px、844 → 321px、超宽横屏封顶 420px。

修复后三档宽度实测（面板高 / 点击后列表可视高）：

| 尺寸 | 修复前面板 | 修复后面板 | 点击后列表 |
| --- | --- | --- | --- |
| 844×390 | 341px ✅ | 341px | 253px |
| 740×360 | **144px** ❌ | **311px** ✅ | 223px |
| 600×360 | **144px** ❌ | **311px** ✅ | 215px |

### 7.5.10 结果面板改成三档可拖拽抽屉

**问题**：竖屏纵向只有 700px 上下，固定的 40vh 面板长期占着近一半屏幕。

**做法**：面板变成三档抽屉，`peek 0.27 / half 0.5 / full 0.82`（占视口高度比）：

- 按住把手上下拖 → 跟手变高，松手**吸附到最近一档**（不停在半路）
- 快速上滑/下滑**轻扫** → 直接到相邻档（不用精确拖到位置）
- 把手可 Tab 聚焦，上下键换档（无障碍）
- 地图模式下被硬压到 168px 且把手收起（进地图时列表本来就该让位）

**分工刻意切开：JS 只写数值，CSS 决定怎么用。**
`panel.js` 往 `#panel` 上写 `--drawer-h`（`'27vh'` 或拖拽中的 `'512px'`）和 `data-drawer`；
`styles.css` 消费它们，决定真实高度、过渡动画、以及"这一屏到底要不要抽屉"。

#### 三个坑，都踩过

**① 高度必须用 `height`，不能用 `max-height`。**
手机布局的网格行是 `auto`（行高由内容决定），而 `max-height` 只能往下压、不能往上撑。
变量写 `82vh`、`max-height` 算出来 692px 全对，面板实际却纹丝不动停在内容高度 **124px** ——
拖了等于没拖。这类"数值全对但没生效"的 bug 只能靠**读实际渲染高度**
（`getBoundingClientRect()`）发现，读 CSS 变量是看不出来的。

**② 「定位后要不要把面板推高」分两种情况，方向相反。**

| 场景 | 该不该推 | 为什么 |
| --- | --- | --- |
| 单条命中（搜索自动定位） | **不推** | 主任务是"看清箱子在哪"，没什么可挑的（面包屑 + 详情卡已说明是哪条），推高只白吃一半屏幕 |
| 多条命中后手动挑一条 | **推** | 用户刚在列表里选了一条，需要同时看见"选的是哪条"和"它在哪" |

一开始无脑"定位后都推到 half"，实测单条命中时 3D 舞台只剩 **324/844 = 38%**，
比改造前的 76% 还差 —— 正好撞在用户投诉的"3D 被挤成一条缝"上。
**功能加对了，方向搞反了，反而更糟。**

**③ 判断放哪一层：要放在"所有路径的唯一收口"。**
`revealResults()` 一开始包在 `main.js` 的 onPick 回调外面，只覆盖"点结果卡"这一条路；
而 `search.js` 里单条命中是 `locate(hits[0])` **直接调用**，绕过了回调 ——
表现为"搜出来高亮了，但结果卡还藏在收起的面板里"。
改放进 `emphasis.js` 的 `locate()` 内部后，三条定位路径（搜索自动定位 / 点结果卡 / 点地图箱子）
全都覆盖到了。**加钩子前先问：这是不是所有路径都会经过的地方？**

#### peek 为什么是 0.27 而不是更小

面板里同时住着四样东西，高度是叠出来的：

```
把手 22 + 面板头 44 + 结果卡 101 + 详情卡标题（收起态）约 50 ≈ 217px
217 / 844 ≈ 0.257
```

早先设 0.22（186px）时，结果卡会被面板下沿**从中间切断**，只剩名字、meta 行看不见 ——
看起来像渲染坏了。peek 的语义就是"刚好够确认搜到的是哪一条"，卡被切一半就失去意义了。

---

## 8. 怎么自己跑一遍验收（可选）

`verify/` 目录里有三份自动化测试脚本，用真实浏览器（Chromium）跑断言：

| 脚本 | 覆盖 | 断言数 |
| --- | --- | --- |
| `verify-refactor.mjs` | 重构后功能无变化（启动/搜索/高亮/复位/鼠标/手机适配） | 22 |
| `verify-mobile-ux.mjs` | Day 8 改动（触摸手感/详情卡收起/全屏地图/全景自适应/无限拖拽/页面锁死/结果面板四态/标记环三态/进地图看全貌/**结果面板抽屉**/**手机横屏三档尺寸**/**结果列表在左**）+ 动画抢断回归 | 110 |
| `verify-api.mjs` | 方案乙：前端确实从后端取数 + 后端挂了能降级 | 7 |

> 手机端那套（110 条）覆盖**五种屏幕形态**，因为它们走的是不同的 CSS 分支：
> 竖屏 390×844（底部抽屉）、横屏 844×390 / **740×360** / **600×360**
> （左侧栏，后两个专门覆盖媒体查询重叠区间）、桌面 1280×800（固定右侧栏）。
> 改布局时这几边都要看 —— 只测一种宽度的话，重叠区的 bug 抓不到（见 8.1 第 7 条）。

**它们需要 Playwright**（只是测试用，运行页面本身不需要）：

```bash
# 一次性安装（在任意空目录）
npm install playwright
npx playwright install chromium

# 跑重构验收（先确保 8010 服务已启动）
node verify/verify-refactor.mjs

# 跑手机端体验验收（先确保 8010 服务已启动）
node verify/verify-mobile-ux.mjs

# 跑方案乙验收（有额外前置条件，见下）
node verify/verify-api.mjs
```

预期输出结尾：`===== 22/22 通过 =====`、`===== 66/66 通过 =====` 与 `===== 7/7 通过 =====`

> **⚠️ `verify-api.mjs` 的两个前置条件**（不满足会失败，**不是代码坏了**）：
> 1. 把 `src/config.js` 的 `DATA_SOURCE` 改成 `'api'`
>    —— 默认是 `'demo'`，那个模式下前端直读本地 JSON，**根本不会发 API 请求**，
>    "前端调用了后端"这条断言必然失败。
> 2. 起后端：`cd backend && python server.py`（监听 8011）
>
> 跑完记得把 `DATA_SOURCE` **改回 `'demo'`**，否则线上静态版会拿不到数据。
> 脚本已内置自检：模式不对时会直接提示，不会让你对着断言瞎猜。

> **⚠️ 后端进程只在"同一条命令内"存活。**
> 沙箱会在 shell 命令返回时清理掉后台子进程，所以
> `python backend/server.py &` 之后**另一条**命令里访问 8011 会失败
> （表现为 `502` 或 `ERR_CONNECTION_REFUSED`）。
> 想验证后端，请把"起后端 + 跑测试"写在**同一条命令**里。

### 8.3 截图去哪了

跑测试会在 `verify/shots/` 下生成截图。这个目录**已加进 `.gitignore`**，不进仓库 ——
理由是截图属于**产物**，跑一遍脚本就能重新生成，没必要占用公开仓库的体积，
也避免"改了界面忘了更新截图"导致仓库里的图和实际不符。

`3d-warehouse/` 根目录下另有 **2 张故意留在仓库里**的交付截图
（`shot-mainview-with-url.png`、`shot-marker-none.png`），它们是打卡清单要求、
需要给人直接看到的，所以在 `.gitignore` 里用 `!` 开了例外。
新增这类交付图时，照 `.gitignore` 里的注释加一行 `!文件名` 即可。

### 8.1 写这套测试踩过的坑（给以后改测试的人）

这三条都花了不少时间才定位，记下来省得重踩：

1. **`page.click(sel)` 在 `body { position: fixed }` 下会一直超时。** Playwright 点击前会做"可点性检查"（等元素稳定、可滚入视口），body 设了 fixed 之后这个检查会**永远认为元素不可点**——即使 `elementFromPoint` 命中的就是目标本身、布局也完全正常（实测元素在 684~717px、视口 727px，没有任何遮挡），照样 5 秒超时。
   → 换成 `tapAt(page, sel)` 工具：先在页面里取元素中心的真实坐标，再用 `page.mouse.click(x, y)` 派发鼠标事件，绕开可点性检查。

2. **裸 `await page.click('...')` 无超时会永久挂住整个测试。** 元素处于 `hidden` 时 Playwright 会一直等下去，没有超时就直接卡死在那一行（当时卡在 31/47）。
   → 所有点击都加 `{ timeout: 5000 }`，或用 `tapAt`（它自己先判断有没有可点元素）。

3. **惯性会污染断言。** 惯性是自跑的 `rAF` 循环，抬手后 `view.theta` 还在变。同一用例两次跑出 `2.17` 和 `3.60` 两个值，断言时好时坏，极难排查。
   → 两条措施：① 旋转量**读"抬手前"的值**（手指实际拖出来的转量，不含惯性）；② 每次手势后调 `window.__api.haltInertia()` 显式掐断。`haltInertia` 由 `controls.js` 模块级导出——因为 `initControls()` 的返回值只有 `main.js` 拿得到，而测试需要随时能停。

> **写这类脚本的三个坑**（踩过了，记下来）：
> 1. 视角事件监听在 `#canvas-host` 里的 `<canvas>` 上，模拟事件必须派发给 **canvas 本身**，派给外层 `div` 到不了。
> 2. Chrome DevTools Protocol 的 `dispatchTouchEvent` 会把连续的 `touchMove` **合并**（实测发 10 个只到 2 个），测"每像素转多少"必须改用页面内构造 `PointerEvent`。
> 3. 比较旋转量**必须用有符号位移**，不能取 `Math.abs`。初始 `theta=0.62` 靠近 ±π 边界，触摸滑 100px 转 `-1.1` 落到 `-0.48`，一取绝对值又变回 `0.48`——恰好把 2.2 倍的差距抹平成"和鼠标一样"，会误判成失败。另外**复位后要等动画真正结束**（或先 `stopFly`）再取初值，否则 `theta` 会被飞行插值写回起点。

> 这份脚本是给"改完代码想确认没改坏"用的。日常只是打开页面看效果，用第 2 节的启动命令即可。

> **4. `setTimeout` 在页面里会被节流，别拿它当"精确时钟"（Day 8 补）。**
> 页面跑着 WebGL 渲染循环，无头环境下 `setTimeout(8)` 实测每步真实耗时约 **180ms**。
> 这会让"模拟快速轻扫"变成慢拖 —— 速度算出来只有 `0.12px/ms`，
> 触发不了"轻扫换档"分支，断言永远失败（而代码其实是对的）。
> → 需要**更快**时用忙等：`const spin=(ms)=>{const t=performance.now();while(performance.now()-t<ms){}}`，
> 不受节流影响。需要**更慢**时反而可以放心用 `setTimeout`（节流只会让它更慢，方向是安全的）。
> 一句话：**用 `setTimeout` 控节奏前先问，节流是帮我还是害我。**

> **5. 断言要读"用户看到的值"，不是"代码写下的值"（Day 8 补）。**
> 抽屉改造时，`--drawer-h` 写成 `82vh`、`max-height` 算出来 692px 全对，
> 但面板实际高度纹丝不动停在 124px（因为网格行是 `auto`，`max-height` 撑不大）。
> 如果断言读 CSS 变量，会一路绿灯地放过一个"拖了没反应"的真 bug。
> → 尺寸类断言统一读 `getBoundingClientRect()`。
> **推论**：断言对象会随着布局重构而失效（比如"面板变高"改成了"结果列表变高"）。
> 这时要问的是"**用户价值还在不在**"，而不是"断言为什么挂了"——
> 价值还在就改度量点，价值没了才是真回归。

> **6. 多开页面会拖慢加载，测完就关（Day 8 补）。**
> 每个页面都占一个 WebGL 上下文 + 一条 rAF 循环。手机页/桌面页/横屏页同时开着，
> 新页面加载会明显变慢甚至超时（实测遇到过横屏页等 25 秒没起来）。
> → 一个页面测完立刻 `close()`，再开下一个。
> 另外这类"等页面就绪"的等待**要用 `try/catch` 包住并报 FAIL**，
> 否则整个脚本会直接崩掉，后面几十条断言一条都不跑，很难定位。

> **7. 媒体查询的"重叠区间"要专门造尺寸去测（Day 8 第二轮补）。**
> 这是本轮最值钱的一条教训。
>
> 布局 bug 常常**只在某些尺寸出现**，因为多个媒体查询的适用条件会重叠，
> 而重叠处的胜负由**优先级**决定（不是书写顺序）。
> 本轮真例：抽屉规则在 `max-width: 820px` 里、横屏规则在
> `orientation: landscape and max-height: 560px` 里，
> **740×360 两个都命中**，抽屉那条优先级更高，把横屏的 `height: 100%` 推翻 ——
> 面板只剩 144px，下面全空白。
>
> 但 **844×390 完全正常**（844 > 820，不匹配抽屉那条）。
> 如果横屏只测 844 这一个尺寸，这个 bug 永远不会被发现。
>
> → 凡是"按宽度/高度分档"的布局，**每个分档边界两侧都要各测一个尺寸**。
> 本项目横屏固定测三档：`844`（不重叠）、`740`（重叠区）、`600`（重叠 + 又命中 600 档）。
> 判断哪里是边界：把 CSS 里所有 `@media` 的条件列出来，
> 找**可能同时为真**的组合，就是必须覆盖的尺寸。
>
> 配套的习惯：**CSS 里的条件要和 JS 里的判断保持一致**。
> 本项目的抽屉由 `panel.js` 的 `drawerApplies()` 决定要不要写 `--drawer-h`，
> CSS 决定怎么消费它 —— 两边条件一旦漂移，就会出现
> "JS 以为该有抽屉、CSS 以为不该有"的错位。所以 CSS 那边刻意写成
> `@media (max-width: 820px) and (orientation: portrait)`，
> 与 JS 的 `max-width: 820px && !(landscape && max-height: 560px)` 表达同一件事。

### 8.2 怎么截一张"带地址栏"的图（Day 8 补）

打卡清单常要求"图里要有地址栏"。这件事比看起来麻烦，因为 **Playwright 截不到浏览器外壳**：

- `page.screenshot()` 和 CDP 的 `Page.captureScreenshot` 都只能拍到 **web 内容表面**（相当于只截视口），拿不到标签页和地址栏。
- 所以必须开**真实窗口**（`headless: false`），再用系统 API 抓窗口像素。

具体做法（脚本已在 `verify/` 用过，思路记下来）：

1. Playwright 以 `headless: false` 启动，`--window-size=1280,1000`（这个尺寸**含**标题栏和地址栏，内容区会少约 90px）。
2. 页面里搜一次词，让结果列表渲染出来，并调 `haltInertia()` 停掉惯性（否则截图瞬间镜头还在转，拍出来角度是随机的）。
3. 用 Win32 的 `GetWindowRect` 拿窗口矩形，再 `BitBlt` 把屏幕该区域搬进内存 DC。
4. 取 DIB 像素后**手写 PNG**（标准库 `zlib` 就够，不用装 Pillow）。

**两个必踩的坑：**

**① 颜色通道转换不能图省事用切片。** 从 DIB 拿到的行数据是 BGRA 交错的。
看起来最自然的写法 `row[2::4] + row[1::4] + row[0::4]` 是**错的** ——
它把**所有** R 字节集中到前面、所有 G 集中到中间、所有 B 集中到最后，
整行像素排列被打散重排，输出变成横向重复的乱码（实测就是这么翻车的）。
必须**逐像素**取三个通道：`for i in range(0, w*4, 4): out += row[i+2], row[i+1], row[i]`。

**② `PrintWindow` 对 Chromium 这种 GPU 合成的窗口不可靠。** 实测对比同一窗口：
`BitBlt` 抓的图横向无重复（正常），`PrintWindow` 加了 `PW_RENDERFULLCONTENT(2)` 后
画面仍被**横向平铺 3 遍** —— 它把窗口画进 DC 时用的行宽和 `GetDIBits` 取回时假设的不一致。
所以用 `BitBlt`（代价是窗口必须真的在屏幕上、不被遮挡，所以要先把窗口挪到固定位置并置前）。

> 验证手段：抓完不要只看缩略图，**程序化检测"第一行里是否出现重复内容"**——
> 比较第 0 行和后续各行的字节，以及第一行内是否存在周期性重复段。
> 当时就是靠这个把"看起来像渲染坏了"的平铺问题定位到颜色通道转换上的。

---

## 9. 在线演示版怎么更新（发布 / 下线）

线上那份演示是**纯静态版本**，从 `3d-warehouse/` 派生出来的一个临时副本目录发布的。
它**不含后端**，用的是内置演示数据。

### 为什么不能发布带后端的版本

发布环境只给**一个 HTTP 端口**。本项目是前后端分离（前端 8010 + 后端 8011），
两个端口无法同时暴露。而当前 `DATA_SOURCE = 'demo'`（离线模式）本来就不需要后端，
所以发布纯静态版即可完整演示 3D 找货效果。

### 更新步骤

```bash
# 1. 在仓库根目录，重新生成发布副本（排除 backend/verify，它们不该公开）
#    只删要替换的项再覆盖，比 rm -rf 整个目录安全，范围明确
for d in src vendor data; do rm -rf _deploy_3d/$d && cp -r 3d-warehouse/$d _deploy_3d/$d; done
for f in index.html styles.css package.json; do cp 3d-warehouse/$f _deploy_3d/$f; done

# 2. 【必做】对副本本身跑一遍冒烟测试（不是对源码目录）
cd 3d-warehouse && bash verify/deploy-smoke.sh

# 3. 跑绿了，再让 AI 发布 _deploy_3d 目录（会复用同一链接，内容被替换）
#    —— 直接跟 AI 说"把 3d-warehouse 重新发布到线上"即可

# 4. 想下线时：跟 AI 说"把线上演示下线"
```

> `_deploy_3d/` 已在 `.gitignore` 中（`_deploy_*/`），不会进仓库。
> **注意**：重新发布会**覆盖**同一个链接的内容，而该链接可能已分享给别人。

### 为什么第 2 步必须对「副本」跑，而不是对源码目录

平时跑验收用的是 `3d-warehouse/` 源码目录，但真正上线的是 `_deploy_3d/` 这个派生副本，
两者只靠一次 `cp` 同步。**漏拷一个文件、或改完源码忘了同步，会出现"源码目录全绿、
线上却是坏的"**——而且这种错最难发现，因为所有本地验证都通过。

`verify/deploy-smoke.sh` 做三件事：

1. 前置检查副本本身：`DATA_SOURCE` 必须是 `'demo'`、目录里不能有 `backend`/`verify`
2. **每次现从 `verify/verify-mobile-ux.mjs` 派生**一份指向 8020 端口的临时副本再跑
   —— 不存派生结果，否则验收脚本加了新断言、冒烟测试还停在旧版本，等于白跑
3. 跑完自动清理临时文件（`trap ... EXIT`，中途出错也会清）

> ⚠️ 脚本里有个坑写死了：**起服务与跑测试必须在同一条命令里**。
> 沙箱会回收上一条命令留下的后台进程，分两次调用的话第二次 `curl` 拿到 `000`。
> 同理 `curl` 访问 localhost 要加 `--noproxy '*'`，否则被 `http_proxy` 环境变量拦掉。

---

*运行说明结论：本页面为纯静态前端，一条 `python -m http.server` 命令即可启动，无需安装依赖；当前使用 26 条演示数据，界面已明示数据来源；后续按 `TECH_DESIGN.md` 方案 X 接入后端读取真实（副本）数据。*
