# ============ Python 运行态 ============
__pycache__/
*.pyc

# ============ 本地私有数据（禁止入库）============
# 若把后端切到 tencent 模式，真实数据快照放这里，不要提交
seed.local.json
data/real-*.json

# ============ 验收脚本的截图产物 ============
# 跑 verify/*.mjs 会往 verify/shots/ 里写图。这些图是**产物不是源码**，
# 随时能重新生成（跑一遍脚本即可），所以不进仓库。
# 好处：公开仓库不会因为图片越攒越多而变大，也避免"改完代码忘了更新截图"
# 导致仓库里的图跟实际界面不一致。
#
# 注意：下面的 ! 例外是**故意**的 —— 打卡清单要求的交付截图要能被人看到，
# 所以留在仓库里。新增交付图时，照这样加一行 ! 例外。
verify/shots/
!shot-mainview-with-url.png
!shot-marker-none.png
