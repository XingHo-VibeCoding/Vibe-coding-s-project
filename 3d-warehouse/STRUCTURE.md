# 代码结构说明（STRUCTURE.md）

> **Day 7 产出**。本文档说明 `3d-warehouse` 的模块划分、依赖规则，以及「想加功能 / 想删功能」时该动哪个文件。
>
> 配图：`STRUCTURE.svg`（可直接用浏览器打开，或用 VS Code / GitHub 预览）

---

## 1. 一句话

**每个文件只做一件事，依赖方向只朝下。** 所以改动是局部的：想加个功能，通常只碰一个文件。

---

## 2. 四层结构

```
                       main.js（组装层 · 172 行）
                             │
       ┌──────────┬──────────┼──────────┬──────────┐
       ▼          ▼          ▼          ▼          ▼
   controls    search    emphasis   panel      api        ← 业务层
       │          │          │          │
       ▼          ▼          ▼          │
    camera ←──────┴──────────┘          │                 ← 渲染层
       │                                 │
       ▼                                 │
     scene ──────────────────────────────┘
       │
       ▼
  config / state / layout                                 ← 基础层
```

| 层 | 文件 | 行数 | 职责 | 一句话记住它 |
| --- | --- | --- | --- | --- |
| **组装** | `main.js` | 172 | 接线 + 绑事件 + 启动 | 「施工队」：把零件拼起来 |
| **业务** | `search.js` | 86 | 输入 → 命中哪些物资 | 「检索规则」 |
| | `emphasis.js` | 98 | 强调哪个箱位 + 镜头聚焦 + 复位 | 「聚光灯」 |
| | `panel.js` | 155 | **界面 DOM 的唯一出入口** | 「显示屏」 |
| | `controls.js` | 123 | 鼠标与触摸手势 → 相机操作 | 「方向盘」 |
| **渲染** | `scene.js` | 237 | 渲染器/相机/灯光/箱位/标签 | 「摄影棚」 |
| | `camera.js` | 142 | 视角参数 → 相机位置；飞行动画 | 「摄像机」 |
| | `minimap.js` | 71 | 小地图（**已停用**，可整删） | 「备用镜头」 |
| | `api.js` | 77 | 对外测试接口 | 「调试面板」 |
| **基础** | `config.js` | 49 | 可调参数（颜色/尺寸/视角） | 「控制台旋钮」 |
| | `state.js` | 44 | 会变的数据 | 「现场记录板」 |
| | `layout.js` | 81 | 区域/排/层 → 世界坐标 | 「地图刻度」 |
| | `adapter.js` | 80 | 外部数据 → 标准结构 | 「翻译官」 |

> 后端不在上图内：`backend/server.py`（261 行）在**数据侧**，前端通过 `adapter.js` 跟它通信。

---

## 3. 依赖规则（最重要的一条）

**箭头只允许向下，不允许反向，不允许成环。**

为什么这条重要：如果 A 依赖 B、B 又依赖 A，改任何一个都可能崩掉另一个，
你就永远不敢动代码了。单向依赖保证「改下层不影响上层」。

### 怎么判断新代码该放哪层

问自己一个问题：**这段代码在回答什么问题？**

| 它在回答… | 放哪层 | 例子 |
| --- | --- | --- |
| 「用户输入 X，应该匹配到哪些数据？」 | 业务层 `search.js` | 加拼音首字母搜索 |
| 「界面上应该显示成什么样？」 | 业务层 `panel.js` | 结果卡片加个字段 |
| 「画面怎么画出来？」 | 渲染层 `scene.js` / `camera.js` | 加个新的灯光 |
| 「某个数值是多少？」 | 基础层 `config.js` | 调整默认视角距离 |

---

## 4. 想加功能，动哪个文件？

| 你的需求 | 改这一个文件 | 会不会牵连别处 |
| --- | --- | --- |
| 支持拼音搜索（如 `bxgcp` → 不锈钢餐盘） | `search.js` | 不会 |
| 结果卡片增加「责任人」字段 | `panel.js` + `adapter.js`（加字段翻译） | 小范围 |
| 换配色 / 改货架尺寸 | `config.js` | 不会 |
| 加一种相机视角（如正面视图） | `camera.js` | 不会 |
| 支持键盘上下键选结果 | `controls.js` + `panel.js` | 小范围 |
| 恢复小地图 | `index.html`（加回结构）+ `main.js`（一行调用） | **引擎代码已在，无需改** |
| 接入真实数据 | `config.js` 改一个常量 + 后端配凭据 | **前端代码不用改** |

---

## 5. 想删功能，动哪个文件？

| 想删掉 | 怎么做 | 风险 |
| --- | --- | --- |
| 小地图 | 删 `minimap.js` + `main.js` 里 2 处 import/调用 | 极低（本就停用） |
| 手机适配 | 删 `styles.css` 里两个 `@media` 块 | 低（只影响窄屏） |
| 整个 2D 俯视能力 | 删 `camera.js` 里 `topView` 分支 + `scene.js` 的 `topCam` | 中（有多处引用） |
| 后端集成 | `config.js` 改回 `'demo'`；`adapter.js` 可留着不用 | 极低 |

---

## 6. 已知技术债（诚实记录）

| 技术债 | 说明 | 影响 | 建议 |
| --- | --- | --- | --- |
| **坐标公式双份** | `backend/server.py` 的 `position_for()` 与 `layout.js` 的 `positionFor()` 各一份 | 改一边忘另一边 → 箱子错位 | 已加注释互指 + 测试校验；彻底解法是后端只返回 `zone/row/level` |
| `topView` 状态残留 | 2D 俯视的入口按钮已从界面移除，但状态位和相机分支还在 | 无功能影响，只是代码冗余 | 确认不需要 2D 后清理 |
| `minimap.js` 停用但保留 | 容器不存在时静默跳过 | 无功能影响 | 确认不需要后整文件删除 |

---

## 7. 与其他文档的关系

| 文档 | 关系 |
| --- | --- |
| `../RUN.md` | 启动方式与验证步骤 |
| `../TECH_DESIGN.md` | 技术路线来源（方案 X：3D 经后端取数） |
| `backend/README.md` | 后端接口、凭据配置、数据缺口说明 |
| `verify/*.mjs` | 自动化验收脚本（改完代码跑一遍，确认没改坏） |
