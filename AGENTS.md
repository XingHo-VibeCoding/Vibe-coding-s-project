# 项目协作规则（AGENTS.md）

> **内容简介**
> 本文件是本项目的人机协作「宪法」。任何 AI 助手（WorkBuddy / Claude / Codex 等）在本目录下工作之前，先读完它。
> 它约束两件事：**AI 怎么配合你**（一～七节），以及**做事的工程底线**（八～十三节，其中十一～十三节为 Day 6 追加）。
> - 你每天会把「今日任务清单」整块发给我，那是当天唯一任务范围（见第一节）。
> - 日常节奏：收到清单 → 我先给执行计划并**停下等你确认** → 一次只做一步 → 每步汇报 → 你说「进入下一板块」才继续 → 收尾逐条核对完成标准。
> - 你是来学的，所以默认「先讲清楚、再动手」；删改、提交、借鉴都要先跟你确认。
> 建立日期：2026-09-16（Day 1）。最近一次更新：2026-09-19（Day 6）追加第十一～十三节规则，并更新第 0 节「当前阶段 / 技术栈」。

---

## 0. 项目概况

| 项目项 | 内容 |
| --- | --- |
| 项目名称 | Vibe Coding 打卡项目 |
| 项目目录 | `D:\Vibe coding打卡\workbuddy工作区` |
| 目标 | 用自然语言驱动 AI 完成一个可运行的小项目，边做边学 |
| 当前阶段 | Day 5 已完成（Day 1 规则立定 → Day 2 建仓 → Day 3 研究 → Day 4 PRD → Day 5 技术设计）；Day 7 起写代码 |
| 技术栈 | 已定（Day 5）：「纯静态前端（原生 HTML/JS + Three.js）+ 自托管 Python 轻后端 + 腾讯文档智能表作数据底座」，详见 `TECH_DESIGN.md` |

---

## 一、任务边界（最重要）

1. 收到我的任务清单后，**这份清单就是今天唯一的任务范围**，只做清单里写的事。
2. 不做清单以外的工作：不建多余的文件、不写清单没提的功能、不顺手优化已有代码。你觉得可以改得更好，先跟我说一句，等我点头。
3. 不提前做以后的天数。只有我把某天的清单发给你、并明确说「现在开始做 Day X」，才允许动那天。
4. 拿不准是否在范围内，先问我，不要猜。

---

## 二、交互式推进

1. 收到清单后，先只给我今天的执行计划：按什么顺序做、每步怎么做、哪些要我自己动手。**写完停下，等我确认。**
2. 确认后**一次只做一个步骤**，绝不连着往下做。
3. 每步做完立刻停下，告诉我三件事：做了什么、改了哪些文件、怎么验证（附我能亲眼确认的结果）。
4. 然后**停下等我回复「进入下一板块」**。我没说这四个字，你就不要继续。
5. 「进入下一板块」只对当前这一步有效，不代表可以一路做到收尾。

---

## 三、人机分工

1. 需要我人工操作的（注册账号、点按钮、装软件、截图等）：不要替我做，也不要假装已完成。只告诉我：这步在干什么、一二三步怎么做、做完我该看到什么、容易踩什么坑，然后停下等我。
2. 需要你生成的内容（写文档、建文件、写代码）：**等我说了再做**，不要在我刚说「开始吧」时就把整天的东西生成出来。
3. 需要拍板的地方（选方案、起名字、定风格），把选项和各自代价列给我选，不要自己决定。
4. 验证结果要我能亲眼确认（页面能打开、文件能看到、命令有输出），不接受「已经完成了」这种口头结论。

---

## 四、讲清楚环节（我是来学的 / 教练模式）

1. 每个步骤开始前，先告诉我：它在整个作品里是什么位置、为什么现在做、做完会变成什么样。
2. 每步用「做了什么 → 结果 → 为什么」讲明白，不要只给结果。
3. 我不懂的概念（命令、报错、术语、配置），一两句话解释清楚，不要默认我会。
4. 报错时先告诉我错在哪、可能什么原因、打算怎么修，我同意后再动手。不要输出教程式长文。
5. **教练模式的边界**：AI 默认只讲解、不直接写应用层代码。可**直接产出**的是——讲解说明、初始化/一键启动脚本（`.bat`/`.ps1`）、目录脚手架、说明文档；**必须等你动手**的是——业务逻辑、SQL 种子数据、页面组件等应用层代码（AI 只给模板 + 逐行讲解）。授权按「批次」算：上一轮批准过某类修改，不等于下一轮同类新问题自动获准，发现问题先报告。

---

## 五、Git 与提交

1. 我授权你代为执行 git 提交和推送，但**提交前必须先列出**本次会改动哪些文件、每个文件属于哪天的任务，等我确认后再提交。
2. commit 信息格式：`Day X｜一句话说明`。
3. 密钥、`.env`、数据库连接串永远不进代码、不进提交。
4. 一天的任务全部做完、我核对完文件清单之后才提交，中途不要边做边提交。

---

## 六、当日收尾

1. 我说「今天做完了」之后，请你逐条对照清单里的「完成标准」，输出一张表：完成标准 / 状态（已完成·部分完成·未完成）/ 证据（文件、页面、命令输出）。
2. 核对必须诚实，没做完、做了一半、跳过没做的，直接标出来并说明原因，不要为了凑「全部完成」而模糊处理。
3. 有未完成项时，告诉我还差什么、接不接着做，**由我决定**，不要自己往下做。
4. 全部完成时，按清单的「收尾指令」整理当天成果，提醒我该交哪张截图、该在群里回什么。

---

## 七、出问题的时候

1. 我要求撤销时，用安全回退（`git revert`，保留历史），禁止 `git reset --hard` 和强制推送。
2. 回退前先和我确认恢复到哪个提交，回退后列出当前状态让我核对。
3. 同一个操作反复失败时停下来报告，说清楚试了什么、报什么错、你判断的原因，不要循环重试。

---

## 八、工程纪律（编码准则）

- **先想后写**：动手前说明假设；不确定就先问。
- **简洁优先**：用最少的代码解决问题，不写投机性代码，不加没人要求的「灵活性」和「可配置性」。
- **精准修改**：只动必须动的地方；不顺手「改进」旁边的代码、注释或格式。注意到无关的死代码 —— 提一嘴就好，别删。
- **目标驱动**：把任务转成可验证的成功标准（例如「写一个能复现 bug 的测试，然后让它通过」），循环验证到通过为止。
- 检验标准：**每一行改动都应该能直接追溯到我的需求。**

---

## 九、做项目前提：先借鉴，再动手

1. 接到新功能 / 新项目时，第一步**不是写代码**，而是先找「有没有可借鉴的现成东西」：本项目历史代码、课程/官方模板、同类开源项目、官方示例。
2. 借鉴优先级：**本项目已有代码 > 课程/官方模板 > 同类开源项目 > 从零自写**。
3. 只借鉴「结构和思路」，不盲目复制；确认它适合当前需求再改。
4. 找不到可借鉴的，明确告诉我「没有现成参考」，再一起讨论从零怎么搭。

---

## 十、安全与阶段门禁

- 任何**删除 / 覆盖 / 批量重命名**操作之前：先列出文件清单、影响范围、风险 → 等我明确确认再执行。
- 破坏性操作之前**先备份**，并告诉我备份放在哪。
- 个人目录（桌面 / 下载 / 文档）**只做只读扫描**，不主动清理、不移动、不删除。
- 每个阶段推进前，**先披露全部风险**，再进入下一阶段。

---

## 十一、错误沉淀：踩过的坑要变成资产（Day 6 追加）

**背景**：同样的坑踩第二次，是纯粹的浪费。

1. 遇到并解决「卡住的问题」（报错、环境坑、配置问题、工具异常）之后，**必须**把「**症状 → 根因 → 解法**」三件事记进项目笔记，不要只记「修好了」。
2. 下次遇到**同类问题前**，先查笔记再动手，不要从零开始重试。
3. 记的位置：项目内部经验记 `.workbuddy/memory/MEMORY.md` 的「环境事实」；当天经过记对应日期的日志。
4. **防的是**：重复踩同一个坑（例如环境级报错反复出现、配置反复调错方向），以及「明明上次解决过，这次又想不起来怎么解」。

---

## 十二、结论必须有据：先读取，再下结论（Day 6 追加）

**背景**：AI 最容易犯的错，是把「印象」当成「事实」说出来。

1. AI 给出的任何**技术结论**——某个字段/文件是否存在、某段代码在哪、某个方案是否可行、某项工作是否已完成——**必须附证据**：文件路径 + 行号 / 命令输出 / 文档引用。
2. 给不出证据时，**必须明说「这是推测，尚未核实」**，不得用肯定语气表述。
3. 涉及**改动或判断前提**的结论（例如「这个文件里没有敏感信息」「这个功能已经实现」），**先读文件再说话**，不允许凭记忆或常识回答。
4. **防的是**：拍脑袋式的错误结论被写进文档或据此动手改代码，导致返工；以及把「应该」「大概」「通常」当成事实陈述。

---

## 十三、改文档后必查红线（Day 6 追加）

**背景**：本项目仓库是**公开**的，红线已经写下，但缺少一个「执行动作」去保证它。

1. 每次改动对外文档（`.md` 等会提交的文件）后，**提交前必须自查**是否含敏感信息：
   - 腾讯文档 `file_id` / sheet_id（正式文档与测试副本均属敏感）
   - 密钥 / Token / 密码 / 连接串
   - 指向正式文档的链接
2. 自查方式：用关键词检索（grep 等）逐一确认，**不接受「我记得没写」**。
3. 一旦发现命中：**立即拦下，不提交**，先清理再走流程。
4. 同时确认 `.env`、真实档案等已在 `.gitignore` 中屏蔽。
5. **防的是**：把敏感 ID 或凭据误写进公开仓库，造成不可逆的信息泄露。

---

## 附录 A：目录约定

| 路径 | 用途 | 状态 |
| --- | --- | --- |
| `AGENTS.md` | 项目规则文件（本文件） | 已建 |
| `notes/` | 学习笔记 | 待建 |
| `src/` | 项目代码 | Day 2 之后再建 |

---

## 附录 B：变更记录

| 日期 | 变更内容 | 说明 |
| --- | --- | --- |
| 2026-09-16 | 建立规则文件，立下沟通 / 协作 / 编码 / 安全四类规则 | Day 1 交付（自动生成初版） |
| 2026-09-16 | 合并用户手写的 7 节交互协议 + 工程/安全补充；新增「内容简介」与「先借鉴做项目前提」 | 本次合并 |
| 2026-09-16 | 删除第九节第 5 条（与第八节重复） | 按用户确认的建议精简 |
| 2026-09-19 | 追加第十一节「错误沉淀」、第十二节「结论必须有据」、第十三节「改文档后必查红线」 | Day 6 交付（用户拍板新增 3 条规则） |
| 2026-09-19 | 更新第 0 节「当前阶段」「技术栈」两项过期内容；更新顶部「最近一次更新」说明 | Day 6 交付（原为 Day 1 占位，已与现状对齐） |
| 2026-09-19 | 新增附录 C「红线自查清单」 | Day 6 余力加练（把第十三节改写成队友可用的检查项） |

---

## 附录 C：交给队友也能用的红线自查清单（Day 6 余力加练）

> **用途**：把第十三节从「规则」变成「照着做就行的动作」。任何人在提交前逐项打勾即可。
> **适用对象**：本项目所有协作者（人 / AI 助手）。
> **执行时机**：改动任何会提交的文件之后、执行 `git commit` 之前。

### 使用方式

1. 复制下方清单到你的工作记录里；
2. 逐项执行并在 `[ ]` 打勾；
3. **任何一项打不上勾 → 停下来，先解决再提交**。

### 提交前红线自查清单

**第一组：敏感信息排查（必做）**

- [ ] **1. 查文档 ID**：在改动过的文件里检索腾讯文档相关标识（`file_id`、`sheet_id`、7 位以上的文档编号串），确认**一个都没有**。
- [ ] **2. 查密钥凭据**：检索 `api_key`、`secret`、`password`、`token`、`Bearer`、`sk-` 等关键词，确认**一个都没有**。
- [ ] **3. 查正式文档链接**：检索 `docs.qq.com` 等文档域名，确认没有指向**正式文档**的链接。
- [ ] **4. 查环境变量文件**：确认 `.env`、`.env.local` 等文件**没有被 `git add`**（`git status` 里不应出现它们）。

**第二组：忽略规则确认（必做）**

- [ ] **5. 确认真实档案被屏蔽**：真实物资档案（261 条）等敏感数据文件在 `.gitignore` 中，且 `git status` 看不到它们。
- [ ] **6. 确认无大文件误入**：`git status` 中无意外的大型数据文件 / 图片 / 缓存目录。

**第三组：改动范围确认（必做）**

- [ ] **7. 确认文件清单已列**：已向项目所有者列出本次改动的**全部文件** + 每个文件**属于哪天的任务**。
- [ ] **8. 确认已获确认**：项目所有者已明确回复同意提交。
- [ ] **9. 确认提交信息合规**：commit 标题格式为 `Day X｜一句话说明`，说明包含「改了什么 / 加了什么」两行。

### 自查命令参考

```bash
# 第一组：敏感信息排查（在项目根目录执行）
grep -rn "file_id\|sheet_id\|api_key\|secret\|password\|token\|Bearer\|sk-" --include="*.md" --include="*.js" --include="*.py" --include="*.html" . | grep -v "^./.workbuddy"
grep -rn "docs.qq.com" --include="*.md" --include="*.js" . | grep -v "^./.workbuddy"

# 第二组：确认忽略规则生效
git status --short
cat .gitignore | grep -E "\.env|\.workbuddy|material"

# 第三组：确认将提交的文件
git diff --cached --stat
```

> ⚠️ **注意**：`.workbuddy/` 目录已被 `.gitignore` 屏蔽，其中的笔记**不会进仓库**——所以检索时排除它，避免把「笔记里记录的红线关键词」误判为泄露。

> ⚠️ **已知误报（务必先读）**：上面的 `grep` 命令会**匹配到自己**——因为清单和命令本身写着 `file_id` / `api_key` 这些词，以及文档里「描述性提及」（如"路径走环境变量"）也会命中。
> **判读方法**：命中行的**内容是"关键词本身"或"对规则的描述"→ 属误报，安全**；命中行出现**具体的字符串值**（一串真实的 7 位以上 ID、`sk-` 开头的长串、真实密钥）**→ 才是真泄露，必须拦下**。
> 首次执行本清单时（Day 6）即遇到此误报，已记录在案，避免下次慌乱。

### 发现命中时怎么办

| 情况 | 动作 |
| --- | --- |
| 敏感信息在**未提交**的文件里 | 立即删除该内容 → 重新自查 → 通过后才提交 |
| 敏感信息**已经提交但未推送** | `git reset --soft HEAD~1` 撤回提交 → 清理 → 重新提交 |
| 敏感信息**已推送到远程** | **立即上报项目所有者**，不要自行处理（可能需改写历史 + 更换凭据） |

---

